server:
  servlet:
    context-path: /api/v1.0


spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gest_maint
    username: root
    password:

  jpa:
    hibernate:
      format-sql: true
      dialect: org.hibernate.dialect.MySQL5InnoDBDialect
      ddl-auto: update
    show-sql: true